# Hooks

Custom hooks extracted from screens. Hooks own state + effects, screens call them.
All exported from `index.ts`. Naming: `use<Domain><Action>`.

See: .knowledge/conventions/state-management.md
