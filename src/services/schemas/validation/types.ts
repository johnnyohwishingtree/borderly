/**
 * Schema Validation Types
 *
 * Re-exports the validation-related types from the shared schema types.
 * Types are defined in ../../types/schema and used throughout the validation module.
 */

// Types used by the schema validator are imported from ../../types/schema
// by consumers directly. This file exists for any validation-specific types
// that may be added in the future.
