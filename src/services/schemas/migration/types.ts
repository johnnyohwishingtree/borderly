/**
 * Schema Migration Types
 *
 * Re-exports the migration-related types from the shared schema types.
 * Types are defined in ../../types/schema and used throughout the migration module.
 */

// Types used by the schema migrator (SchemaMigration, SchemaValidationResult, CountryFormSchema)
// are imported from ../../types/schema by consumers directly. This file exists for any
// migration-specific types that may be added in the future.
