# Schemas

Country form schemas (JSON). Every field needs `autoFillSource` for smart delta.
Date fields use `type: "date"`, not `type: "text"`.

See: .knowledge/domain/form-engine.md
See: .knowledge/patterns/add-country.md
