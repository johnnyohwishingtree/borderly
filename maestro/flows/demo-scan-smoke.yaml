# Smoke test using demo passport scan
#
# Faster than the manual entry smoke test — uses instant demo scan
# instead of typing all passport fields.
#
# Tests: Welcome → Tutorial → Demo Scan → Confirm → Companions → Biometric → Main
appId: com.borderly.app
---
- clearState
- launchApp
- runFlow: subflows/onboard-with-demo-scan.yaml
