# Fix Log

## Attempt 1 (2026-03-22)

### Errors Fixed

1. **`_removed` unused variable** (`__tests__/services/tripTemplateService.test.ts:134`)
   - Added `void _removed;` after the destructuring to suppress the lint error

2. **`DEFAULT_PROFILE` unused import** (`e2e/tests/trip-templates.spec.ts:12`)
   - Removed `DEFAULT_PROFILE` from the import since it wasn't used in the file

3. **`exactOptionalPropertyTypes` typecheck error** (`__tests__/components/forms/FormField.test.tsx:1122`)
   - Fixed by conditionally spreading `validation` only when it's not `undefined`, using `...(validation !== undefined ? { validation } : {})`

4. **`submission-tracking.spec.ts` orphaned E2E test** (`playwright.config.ts`)
   - Added `submission-tracking.spec.ts` to the `performance` project's `testMatch` array

### Result
- `pnpm typecheck`: PASS (0 errors)
- `pnpm test`: PASS (5244 tests, 0 failures)
- Pushed: a91dfe5
