# Fix Log

## Attempt 1 — 2026-03-18

**Error:** `react-hooks/exhaustive-deps` lint error in `src/app/App.tsx:103` — `triggerSchemaUpdateCheck` missing from `useEffect` dependency array.

**Fix:** Added `triggerSchemaUpdateCheck` to the dependency array: `}, [triggerSchemaUpdateCheck]);`

**Result:** `pnpm typecheck` ✅, `pnpm test` ✅ (120 suites, 3603 tests). Pushed.

## Attempt 2 — 2026-03-18

**Error:** Metro bundle check (iOS) failed — `Unable to resolve module crypto` from `src/services/schemas/schemaUpdateService.ts:51`. The `computeSHA256` function had a Node.js fallback `require('crypto')` that Metro cannot bundle (Node built-ins aren't available in React Native).

**Fix:** Removed the `require('crypto')` fallback from `computeSHA256`. Hermes (React Native JS engine) and all browsers support `SubtleCrypto` natively. Jest tests mock `schemaUpdateService` entirely so `computeSHA256` is never called in tests.

**Result:** Metro bundle ✅, `pnpm typecheck` ✅, `pnpm test` ✅ (120 suites, 3603 tests). Pushed.
