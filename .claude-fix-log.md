# Fix Log

## Fix 1 — 2026-03-22

**Problem:** Two E2E tests in `deadline-reminders.spec.ts` were failing:
- `Trip Readiness summary is visible for a trip with a future departure`
- `Trip Readiness summary shows correct ready count for multi-leg trip`

Both looked for `trip-readiness-summary` testID and expected text like "Trip Readiness:" / "1 of 2 legs ready".

**Root cause:** PR #620 replaced the plain-text "Trip Readiness: X of N legs ready" summary with the `ReadinessChecklist` component. The old `trip-readiness-summary` testID no longer exists; the new component uses `readiness-checklist` / `readiness-checklist-loading`.

**Fix:** Updated both tests to use `page.getByTestId('readiness-checklist').or(page.getByTestId('readiness-checklist-loading'))` and removed the stale text assertions.

**Checks:** `pnpm typecheck` ✅, `pnpm test` ✅ (169 suites, 4736 tests). Pushed.

## Fix 2 — 2026-03-22

**Problem:** Merge conflict with master in `src/screens/trips/TripDetailScreen/TripDetailScreen.tsx`.

**Root cause:** Master branch (#625) colocated screens into named folders and switched imports to `@/` path aliases. This branch used relative imports and the old flat-file layout.

**Fix:** Resolved conflict by keeping `@/` path aliases (master style) while retaining `ReadinessChecklist` import and `useTripReadiness` hook from this story branch.

**Checks:** `pnpm typecheck` ✅, `pnpm test` ✅ (170 suites, 4740 tests). Pushed.
