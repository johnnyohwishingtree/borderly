# Fix Log

## Fix 1 — 2026-03-22

### Problem
Two CI failures:
1. `RestoreBackupModal.a11y.test.tsx` — TS2322 errors: component no longer accepts `visible`/`onClose` props (refactored from modal to full-screen)
2. `BiometricSetupScreen.tsx` — TS2769: navigates to `NotificationPermission` which was missing from `OnboardingStackParamList`

### Root Cause
`RestoreBackupModal` was refactored into a full-screen component (no props, uses `useNavigation`) with a completely different hook interface (`step`-based flow vs old `isLoading`/`error`/`isSuccess` approach). The a11y test file was not updated to match.

`BiometricSetupScreen` called `navigation.navigate('NotificationPermission')` but that route wasn't registered in `OnboardingStackParamList`.

### Fix
- Rewrote `__tests__/components/settings/RestoreBackupModal.a11y.test.tsx`:
  - Removed `visible` and `onClose` props from all render calls
  - Updated hook mock to match new `UseBackupRestoreResult` interface
  - Added mocks for `@react-navigation/native` and `useProfileStore`
  - Updated all testIDs and assertions to match the new component structure
  - Added tests for new steps: idle, passphrase, loading, error, success, confirming-replace
- Added `NotificationPermission: undefined` to `OnboardingStackParamList` in `src/app/navigation/types.ts`

### Checks
- `pnpm typecheck` — PASS (0 errors)
- `pnpm test` — PASS (4545 tests, 160 suites)
