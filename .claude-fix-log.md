# Fix Log

## Fix 1 (2026-03-22)

**Error:** `TS2379` in `__tests__/components/forms/FormField.test.tsx:1122`

**Root cause:** With `exactOptionalPropertyTypes: true`, passing `validation: undefined` explicitly to a function expecting `Partial<FilledFormField>` is a type error. The `makeNumericField` helper passed the `validation` parameter directly, which could be `undefined`.

**Fix:** Changed `validation,` to `...(validation !== undefined ? { validation } : {})` — a conditional spread that only includes the property when it has a value.

**Checks:** `pnpm typecheck` ✅, `pnpm test --testPathPattern="FormField"` ✅ (116 tests passed)

## Fix 2 (2026-03-22)

**Error:** 3 failing tests in `TripDetailScreen.test.tsx` — `edit-leg-passport-validity-warning` and `new-leg-passport-validity-warning` testIDs not found; `usePassportValidity` not called with `{ countryCode: 'JPN' }`.

**Root cause:** `LegFormSection` (internal component in `TripDetailScreen.tsx`) was missing the `usePassportValidity` hook call and `PassportValidityWarning` render that the tests expected.

**Fix:** Added imports for `usePassportValidity` and `PassportValidityWarning`; called the hook inside `LegFormSection` with `{ countryCode: legData.destinationCountry }`; rendered `PassportValidityWarning` with `testID={`${testIDPrefix}-passport-validity-warning`}` when warning data is non-null.

**Checks:** `pnpm typecheck` ✅, `pnpm test` ✅ (5247 tests passed)
