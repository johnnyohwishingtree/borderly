# Fix Log

## Fix attempt 1 — 2026-03-22

### Errors fixed

**1. TS2379 in `__tests__/components/forms/FormField.test.tsx:1122`**

`makeNumericField` was passing `validation` (which could be `undefined`) directly as an object property to `makeField`. With `exactOptionalPropertyTypes: true`, you cannot explicitly pass `undefined` to an optional property.

Fix: conditionally spread `validation` only when it's not `undefined`:
```ts
...(validation !== undefined ? { validation } : {})
```

**2. Failing tests in `__tests__/screens/trips/TripDetailScreen.test.tsx`**

Three tests expected `edit-leg-passport-validity-warning` and `new-leg-passport-validity-warning` testIDs, but the `LegPassportWarning` sub-component and its imports were removed during the merge of the Duplicate Trip feature (#714) with master's passport validity warning feature (#723).

Fix: Restored `LegPassportWarning` component, `usePassportValidity` and `PassportValidityWarning` imports, and the passport validity warning JSX inside `LegFormSection`.

### Result

- `pnpm typecheck` — PASS (0 errors)
- `pnpm test` — PASS (5247 tests, 190 suites)
- Pushed to `tmp/vf-claude-issue-714-20260322-2152-23415018245`
