# Fix Log

## Attempt 1 (2026-03-22)

### Problem
`noOrphanedE2ETests` lint test failed because `theme.spec.ts` was added in master (#682) but never registered in `playwright.config.ts`.

The checksum mismatch console.error messages in the test output were NOT failures — they are expected console output from tests intentionally testing checksum mismatch handling.

### Fix
Added `'theme.spec.ts'` to the `chromium` project's `testMatch` array in `playwright.config.ts` (alongside `settings.spec.ts`).

### Result
- `pnpm typecheck`: PASS (0 errors)
- `pnpm test`: PASS (5047 tests, 179 suites)
- Pushed to branch
