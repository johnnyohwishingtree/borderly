# Fix Log

## Fix Attempt 1 (2026-03-21)

### Problem
`e2e/tests/tripAndSubmit.spec.ts` failing with:
```
Error: strict mode violation: getByText(/JOHN MICHAEL SMITH/) resolved to 2 elements:
  1) <div class="...text-green-700">JOHN MICHAEL SMITH</div>  (traveler tab chip, hidden)
  2) <div class="...text-gray-700">For: JOHN MICHAEL SMITH</div>  (visible)
```

### Root Cause
After the DeadlineService feature was added (commit `bf07827`), the SubmissionGuideScreen now renders two elements containing "JOHN MICHAEL SMITH":
1. A traveler tab chip (green text, hidden off-screen)
2. The "For: JOHN MICHAEL SMITH" label (visible)

Playwright's strict mode requires `getByText()` to resolve to exactly one element.

### Fix
Changed `page.getByText(/JOHN MICHAEL SMITH/)` to `page.getByText('For: JOHN MICHAEL SMITH')` — exact match on the visible label.

### Checks
- `pnpm typecheck`: PASS
- `pnpm test`: PASS (4294 tests)
- `pnpm lint`: Pre-existing failures (1604 problems, 13 errors) — NOT caused by this change
- E2E test `tripAndSubmit.spec.ts`: PASS locally
