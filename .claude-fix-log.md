# Fix Log

## Attempt 1 — 2026-03-23

### Root causes
1. **`SearchableSelect` undefined in test mock** — `LegFormSection` renders `SearchableSelect` (for airport picker) but the `@/components/ui` mock in `TripDetailScreen.test.tsx` didn't include it, causing a crash when pressing Edit on a leg.
2. **Individual address inputs instead of `AddressAutocomplete`** — The merge from master (`8261cea`) added `AddressAutocomplete` to `TripDetailScreen`, but the branch's modifications to the same screen during the merge lost that change. Tests expected `AddressAutocomplete` with testID `*-accommodation-address` and no city/postal inputs, but screen had the old individual inputs.
3. **Missing `Templates` registry entry** — `TemplatesScreen` was added by the branch but not registered in `maestro/generator/screenRegistry.ts`, causing `screen-registry-sync` structural test failure.

### Fixes applied
- `__tests__/screens/trips/TripDetailScreen.test.tsx`: Added `SearchableSelect` to the `@/components/ui` mock
- `src/screens/trips/TripDetailScreen/TripDetailScreen.tsx`:
  - Added `AddressAutocomplete` to ui imports
  - Added `Address` to profile types import
  - Added `onAddressChange?: (address: Address) => void` to `LegFormSectionProps`
  - Updated `LegFormSection` to use `AddressAutocomplete` instead of individual address inputs
  - Passed `onAddressChange={editHook.updateEditLegAddress/updateNewLegAddress}` to both LegFormSection usages
- `maestro/generator/screenRegistry.ts`: Added `Templates` screen entry

### Results
- `pnpm typecheck`: PASS
- `pnpm test`: 5364 passed, 0 failed
