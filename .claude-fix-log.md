# Fix Log

## Attempt 1 — 2026-03-23

### Problems fixed

1. **E2E test failure** (`trip-templates.spec.ts`): Test looked for `testID="templates-nav-button"` but
   `TripListScreen` header button had `testID="from-template-button"`. Fixed by renaming the testID
   in `TripListScreen.tsx`.

2. **TypeScript errors** in `TripDetailScreen.tsx`:
   - `TravelerSelector` was imported but not used (TS6133) — removed from import.
   - `LegFormSection` destructured `travelers` and `onToggleTraveler` which are not in
     `LegFormSectionProps` (TS2339 + TS6133) — removed from destructure.

### Checks after fix
- `pnpm typecheck` — ✅ zero errors
- `pnpm test` — ✅ 5404 tests passed
- Pushed commit `6fd206e`
