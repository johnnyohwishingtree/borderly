# Fix Log

## Fix attempt 1 — 2026-03-23

### Problem
`__tests__/structure/no-space-x.test.ts` failed because two files still used `space-x-2`:
- `src/screens/trips/TripListScreen/TripListScreen.tsx:184`
- `src/screens/trips/TemplatesScreen/TemplatesScreen.tsx:175`

These were missed in the original `refactor: replace all space-x-* with gap-*` commit.

### Schema error messages
The `[SchemaUpdateService] fetchSchema failed` console messages in the CI output are NOT test failures — they are expected `console.warn` calls inside tests that intentionally pass wrong checksums to verify error handling. All 36 schema service tests pass.

### Fix
Replaced `space-x-2` → `gap-x-2` in both files.

### Checks
- `pnpm typecheck` ✅ (zero errors)
- `pnpm test --testPathPattern="no-space-x|schemaUpdateService"` ✅ (36 passed)
- Pushed to branch ✅
