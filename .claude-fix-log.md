# Fix Log

## Attempt 1 — 2026-03-23

### Problem
16 tests in `TripDetailScreen.test.tsx` were failing because:
1. `LegFormSection` used individual `Input` components for address (line1/city/postal) but tests expected `AddressAutocomplete`
2. `SearchableSelect` was used in `LegFormSection` (for airport) but not mocked in the test, causing render crash when the leg edit form opened

### Fix
- `src/screens/trips/TripDetailScreen/TripDetailScreen.tsx`:
  - Added `AddressAutocomplete` to imports from `@/components/ui`
  - Updated `LegFormSectionProps` to include `onUpdateAddress` callback
  - Updated `LegFormSection` to accept and use `onUpdateAddress`
  - Replaced individual address `Input` components with `AddressAutocomplete`
  - Passed `updateEditLegAddress`/`updateNewLegAddress` from `useEditTrip` hook to `LegFormSection`
- `__tests__/screens/trips/TripDetailScreen.test.tsx`:
  - Added `SearchableSelect` mock to the `@/components/ui` mock

### Result
All 5360 tests pass. Pushed commit f631f57.
