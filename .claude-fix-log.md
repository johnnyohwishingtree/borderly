# Fix Log

## Attempt 1 (2026-03-20)

### Root causes identified

1. **`tripCreation.spec.ts:107`**, **`fullJourney.spec.ts:148`**, **`tripAndSubmit.spec.ts:21`**: The `leg-0-arrival-airport` field was changed from a plain `Input` to a `SearchableSelect` in `CreateTripScreen.tsx`. Tests were still using `.fill('NRT')` and `.toHaveValue('NRT')` which don't work on a Pressable-based component.

2. **`profile.spec.ts:141`**: Test asserted `page.getByText('Family Members').first()` after navigating from Profile → FamilyManagement. Both screens have "Family Members" text. After clicking, Profile unmounts and FamilyManagement shows a loading spinner before showing its heading — causing a brief window where no "Family Members" text is visible, causing a timeout.

### Fixes applied

- **`e2e/helpers/actions.ts`**: Updated `createJapanTrip` to use SearchableSelect interaction (click trigger → fill search → click option) for airport field. This fixes `tripAndSubmit.spec.ts`.
- **`e2e/tests/tripCreation.spec.ts`**: Updated `can fill in destination details` test similarly.
- **`e2e/tests/fullJourney.spec.ts`**: Updated assertion from `.toHaveValue('NRT')` to `.toContainText('Tokyo Narita')` on the trigger element.
- **`e2e/tests/profile.spec.ts`**: Changed assertion to check "Manage your family travel profiles" (unique to FamilyManagement) with 10s timeout instead of "Family Members" (shared with Profile screen).

### Checks status
- `pnpm typecheck`: PASS (0 errors)
- `pnpm test`: PASS (4132 tests, 141 suites)
- `pnpm e2e`: Not run locally (requires full browser), pushed to CI
