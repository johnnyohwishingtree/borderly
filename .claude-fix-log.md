# Fix Log

## Fix 1 (2026-03-20)

**Problem:** `__tests__/integration/canGbrUsaFormPipeline.test.ts` failing for smart delta tests in CAN, GBR, USA.

**Root cause:** Commit `e1664b7` changed `formEngine.ts` so boolean fields with `false` default are treated as "answered" (needsUserInput = false). The THA and VNM tests were updated, but `canGbrUsaFormPipeline.test.ts` was not.

**Affected fields:**
- CAN: `previousNames`, `dualCitizenship`, `criminalOffence` (all boolean)
- GBR: `criminalRecord`, `immigrationBreach`, `ukRefusal`, `terrorismAssociation` (all boolean)
- USA: `aliases`, `mentalDisorder`, `terrorism` (all boolean)

**Fix:** Updated assertions in `canGbrUsaFormPipeline.test.ts` to use `not.toContain` for boolean fields (matching pattern from `formEngine.tha.test.ts` and `formEngine.vnm.test.ts`).

**Status:** All 4231 tests pass, typecheck passes. Pushed as f6a01e2.
