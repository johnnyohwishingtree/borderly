# Fact: Shell Scripts Produce Identical Results to LLM Agents for Deterministic Tasks at Zero Token Cost

For tasks with deterministic outputs — file path tracking, grep, git diff, file append, counting — a shell script produces the same result as an LLM agent. The LLM adds cost (tokens) without adding judgment. LLM agents are only valuable when the task requires interpretation, synthesis, or decision-making.
