# Fact: Parameterized Tests Dominate the Test Count

2,080 of 10,222 tests (20%) are parameterized country mapping validators generated from 14 files. They check structural properties (field IDs exist, selectors are valid CSS) but never verify that auto-fill produces the correct value for a given profile. Another 582 are schema validation tests using similar patterns.
