# Knowledge (legacy — mostly migrated)

Most policies absorbed into structural test JSDoc headers in `__tests__/structure/`.
Only `policies/architecture/agent-token-efficiency.md` remains here.

Previously graduated:
- Models → TypeScript types (src/types/)
- Beliefs → src/config/beliefs.ts
- Facts → .context/external/
- Decisions → .context/decisions/
- Patterns → .context/patterns/
- Policies → structural test JSDoc headers (__tests__/structure/)
