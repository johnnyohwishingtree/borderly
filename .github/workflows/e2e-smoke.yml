name: E2E Smoke Tests

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  actions: write

jobs:
  # Shared webpack build — cached and reused by all test jobs
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node and pnpm
        uses: ./.github/actions/setup-node
        with:
          cache: 'true'
      - name: Build webpack bundle
        run: npx webpack --config webpack.config.js
      - name: Upload bundle
        uses: actions/upload-artifact@v4
        with:
          name: e2e-bundle
          path: e2e/dist/
          retention-days: 1

  # Core chromium tests (24 tests) + QR workflow (7 tests)
  test-chromium:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node and pnpm
        uses: ./.github/actions/setup-node
        with:
          cache: 'true'
      - name: Install Playwright Chromium
        run: npx playwright install chromium
      - name: Download bundle
        uses: actions/download-artifact@v4
        with:
          name: e2e-bundle
          path: e2e/dist/
      - name: Run chromium + QR tests
        run: npx playwright test --reporter=line
        env:
          CI: true
          E2E_PROJECT: chromium,qr-workflow,account-setup,portal-submission,family,country-submissions

  # Performance tests (9 tests) — run in parallel with chromium
  test-performance:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node and pnpm
        uses: ./.github/actions/setup-node
        with:
          cache: 'true'
      - name: Install Playwright Chromium
        run: npx playwright install chromium
      - name: Download bundle
        uses: actions/download-artifact@v4
        with:
          name: e2e-bundle
          path: e2e/dist/
      - name: Run performance tests
        run: npx playwright test --reporter=line
        env:
          CI: true
          E2E_PROJECT: performance

  # Cross-browser smoke (firefox + mobile) — run in parallel
  test-cross-browser:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node and pnpm
        uses: ./.github/actions/setup-node
        with:
          cache: 'true'
      - name: Install Playwright browsers
        run: npx playwright install chromium firefox
      - name: Download bundle
        uses: actions/download-artifact@v4
        with:
          name: e2e-bundle
          path: e2e/dist/
      - name: Run cross-browser smoke tests
        run: npx playwright test --reporter=line
        env:
          CI: true
          E2E_PROJECT: firefox-smoke,mobile-smoke

  # Capture screenshots when UI files change (runs in parallel with tests)
  capture-screenshots:
    needs: build
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Check for UI file changes
        id: changes
        run: |
          CHANGED=$(git diff --name-only origin/master...HEAD -- \
            'src/screens/' \
            'src/components/' \
            'e2e/tests/captureScreenshots.spec.ts' \
            'src/app/navigation/' \
            'src/schemas/' \
          | head -1)
          if [ -n "$CHANGED" ]; then
            echo "ui_changed=true" >> $GITHUB_OUTPUT
          else
            echo "ui_changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Set up Node and pnpm
        if: steps.changes.outputs.ui_changed == 'true'
        uses: ./.github/actions/setup-node
        with:
          cache: 'true'

      - name: Install Playwright Chromium
        if: steps.changes.outputs.ui_changed == 'true'
        run: npx playwright install chromium

      - name: Download bundle
        if: steps.changes.outputs.ui_changed == 'true'
        uses: actions/download-artifact@v4
        with:
          name: e2e-bundle
          path: e2e/dist/

      - name: Capture screenshots
        if: steps.changes.outputs.ui_changed == 'true'
        run: npx playwright test captureScreenshots --reporter=line --workers=1
        env:
          CI: true
          E2E_PROJECT: screenshot-capture

      - name: Commit screenshots
        if: steps.changes.outputs.ui_changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          # Check if any screenshots changed
          git add e2e/screenshots/
          if git diff --cached --quiet; then
            echo "No screenshot changes to commit"
            exit 0
          fi

          git config user.name "Claude CI"
          git config user.email "claude-ci@users.noreply.github.com"
          git commit -m "chore: update screenshots from CI capture"
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git"
          git push

  # Auto-fix on failure (runs after all test jobs)
  notify:
    needs: [test-chromium, test-performance, test-cross-browser]
    if: failure()
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4

      - name: Setup pipeline TypeScript
        uses: ./.github/actions/setup-pipeline-ts

      - name: Dispatch verify-and-fix on PR failure
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          PR_NUM=${{ github.event.pull_request.number }}
          BRANCH="${{ github.head_ref }}"

          # Opt out with the "no-autofix" label
          LABELS=$(gh pr view "$PR_NUM" --repo "${{ github.repository }}" --json labels -q '[.labels[].name] | join(",")' 2>/dev/null || echo "")
          if [[ "$LABELS" == *"no-autofix"* ]]; then
            echo "PR has no-autofix label — skipping"
            exit 0
          fi

          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          FAILED_JOBS=$(gh run view ${{ github.run_id }} --json jobs \
            -q '[.jobs[] | select(.conclusion == "failure") | .name] | join(", ")' 2>/dev/null || echo "unknown")

          npx tsx .github/scripts/lib/cli/pipeline.ts comment "$PR_NUM" "E2E smoke tests failed (${FAILED_JOBS}). Dispatching verify-and-fix with auto-retry. [View run]($RUN_URL)"

          npx tsx .github/scripts/lib/cli/pipeline.ts dispatch "verify-and-fix.yml" \
            -f branch="$BRANCH" \
            -f issue_number="$PR_NUM" \
            -f checks="e2e" \
            -f fix_enabled="true" \
            -f max_attempts="3" \
            -f fix_context="E2E smoke tests failed on PR #${PR_NUM}. Failed jobs: ${FAILED_JOBS}. Run: ${RUN_URL}. Common E2E issues: native modules need web mocks in e2e/mocks/ + aliases in webpack.config.js."

      - name: Dispatch verify-and-fix on master push failure
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          FIX_BRANCH="fix/master-e2e-$(date +%Y%m%d-%H%M)"
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          FAILED_JOBS=$(gh run view ${{ github.run_id }} --json jobs \
            -q '[.jobs[] | select(.conclusion == "failure") | .name] | join(", ")' 2>/dev/null || echo "unknown")

          # Create fix branch from master
          git checkout -b "$FIX_BRANCH"
          git push -u origin "$FIX_BRANCH"

          npx tsx .github/scripts/lib/cli/pipeline.ts dispatch "verify-and-fix.yml" \
            -f branch="$FIX_BRANCH" \
            -f checks="e2e" \
            -f fix_enabled="true" \
            -f max_attempts="3" \
            -f create_pr="true" \
            -f fix_context="E2E failed on master after merge. Failed jobs: ${FAILED_JOBS}. Run: ${RUN_URL}. Common E2E issues: native modules need web mocks in e2e/mocks/ + aliases in webpack.config.js."
